# Manuscript Status

- **Repo type:** Tool / library — multi-frequency time series analysis toolkit
  built on the sktime ecosystem (per root `README.md` and `pyproject.toml`).
- **Why no publication-target manuscript applies today:** evidence checked —
  the repo is a software package (`src/`, `tests/`, `scripts/`) with an
  extensive `docs/` hub (architecture, API reference, configuration, module
  guides); no analysis outputs, figures of record, or research findings exist
  in the tree.
- **What would trigger creating a `manuscript/`:** publishing an applied study
  that uses the toolkit on a real dataset (e.g. forecasting benchmark or
  domain case study) with outputs and figures of record.
