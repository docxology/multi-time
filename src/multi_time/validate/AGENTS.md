# Agents — validate/

## Conventions
- All functions are fully typed with Python 3.10+ annotations
- Module-level `logging.getLogger(__name__)` for structured logging
- Public API re-exported via `__init__.py`
- Tested in `tests/validate/`
